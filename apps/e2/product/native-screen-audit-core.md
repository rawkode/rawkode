# Native core screen audit

2026-09-13. Read-only audit of current SwiftUI source. No new runtime screenshots or accessibility session were taken for this audit; the parent is fixing task creation in the daily editor first. Previous simulator screenshots do not establish the current render. Apply the existing SwiftUI UI-patterns and macOS patterns guidance. Keep the accepted day timeline, calendar colors, sidebar, Today action, and note pull-up; do not introduce another navigation system.

## Order of work

1. Prove that a task created inside the daily note becomes the same task in the task workspace, survives reload, and can be completed from either surface. This is the release blocker; screen polish must not displace it.
2. Repair missing destinations and misleading loading/save/conflict states.
3. Remove duplicate navigation and implementation explanations; use existing native forms, lists, toolbar actions, sheets, search, and date formatting.
4. Validate each screen in Dawn/Dark, at accessibility text sizes, with VoiceOver, and with offline/empty/error data. A build does not validate these interactions.

## Prioritized findings

| Priority | Screen and evidence | Defect or unnecessary element | Conventional replacement and acceptance |
| --- | --- | --- | --- |
| P1 | iPad workspace: `WorkspaceView.swift:29,53-73,92-100` | Regular-width iPad switches to desktop layout, but Voice toolbar is macOS-only and neither Voice nor Meeting capture is a sidebar destination. Features available on the phone disappear on iPad. | Use the existing split-view sidebar/toolbar to expose both destinations on iPad. In portrait, landscape, and Stage Manager widths, each must be reachable without rotating or resizing. Preserve current phone navigation. |
| P1 | Captures: `CaptureViews.swift:54-61` | The most prominent per-item action sends content to legacy device notes, while the current daily note is the shared web document. This reinforces two competing notebooks and fails the user's expected destination. | Make an explicit Add to today's note action use the existing canonical editor/capture integration once task creation is fixed. Keep legacy device-note export as a clearly named secondary action until migration is supported. One tap must add once, acknowledge success, and never report shared save when only local storage succeeded. |
| P1 | Populated captures: `CaptureViews.swift:43-67` | Empty state offers capture, populated phone list has no New action. A user must navigate back home and reopen the sidebar to add another thought. | Add the standard toolbar compose action to the populated list. It opens the existing CaptureSheet, focuses the editor, saves, and returns to the same list position. No new composer type. |
| P1 | Tasks conflict: `TasksView.swift:50-61,119` | UI says the edit needs review, but only offers Discard. The current task row is disabled while a pending edit exists, so the user cannot actually review/reapply through the editor. | Open a normal task detail/form showing Current and Your changes, with explicit Keep current or Apply my changes using the latest revision. Never silently overwrite. Verify 409 retains the draft and both resolution paths are reachable with touch and VoiceOver. |
| P1 | Task capture save: `TasksView.swift:159-167`, `TasksStore.save` | The task is persisted locally before sync, but the modal awaits network completion and keeps Save/Cancel disabled. A slow connection makes durable local capture feel stalled. | Dismiss after confirmed local persistence and run the existing outbox asynchronously. Display concise pending status in the list; disk failure keeps the form and draft open. Test a deliberately delayed server: return to list immediately after local save, then show acknowledgement or pending state accurately. |
| P1 | Tasks initial load: `TasksView.swift:66-72` | Heading says Today/Inbox 'is clear' while the description says 'Loading your tasks'. This presents success before data is known. | Use ProgressView for uncached initial loading. Show an empty state only after a successful empty result, or explicitly say no tasks are saved offline. Cached tasks stay visible during refresh. |
| P1 | Task creation signed out: `TasksView.swift:79-89,159-164` | New task is always enabled although a never-connected account cannot own/persist it; failure appears only after entry and Save. | For no cached or verified owner, make the existing native account action explicit before task entry. Existing-owner offline capture remains supported. Test a clean install and an offline previously signed-in account separately. |
| P2 | Task collections and search: `TasksView.swift:20-42,87` | Four long segment labels must fit all widths, and search silently searches only the selected collection. A task in Inbox disappears from a Today search with no scope explanation. | Use the existing native Picker with an adaptive menu at narrow/accessibility widths. Make search scope explicit or search all tasks while showing their collection. Test a long title, largest supported text size, and a search whose match exists only in another collection. |
| P2 | Task dates and form: `TasksView.swift:114,144-149` | Rows expose ISO storage dates and the date toggle says 'Give it a day'. New-task field is not focused, adding a tap to capture. | Keep date storage unchanged; format display with localized calendar dates (Today/Tomorrow/date). Use a clear Due date label and focus the new-task title. Validate day boundaries in two time zones and long localized date strings. |
| P2 | Phone navigation/search: `PhoneTodayView.swift:171-187,222-236` | Empty Search reproduces the sidebar's full navigation catalogue, including Settings and local notes. Two separately maintained menus provide the same routes. | Keep navigation in the accepted sidebar. Empty search should present recent searches, an explanatory search prompt, or meaningful recent results. Preserve contextual results and one standard search dismissal. Verify every removed browse destination remains available from the sidebar. |
| P2 | Settings/account: `SettingsView.swift:16-31,46-52` | Hostname leads instead of account identity. Several paragraphs explain web-vs-device internals and Watch acknowledgement semantics on the main settings screen. Sign-in offers manual Check connection despite automatic verification. | Lead with the signed-in email/name and concise sync state. Keep Appearance, Account, and Data as native grouped sections. Move detailed storage behaviour to a Data detail page. Keep manual verification as an error recovery action, not the routine sign-in completion path. Verify successful sign-in dismisses automatically and failure provides one actionable message. |
| P2 | Calendar accessibility and unknown times: `DayTimelineView.swift:137-160,197-222` | Unknown-time events are grouped under All-day. Accessible timeline scrolls to the current event only after a recenter change, unlike the canvas's initial scroll. | Separate genuine all-day events from 'Time unavailable'. On first accessible-list display, use its existing ScrollViewReader to reveal the current/next event. Test an unknown-time event and initial launch with accessibility text enabled. Preserve calendar colors and event ordering. |
| P2 | Daily editor loading: `TodayView.swift:30-36,56` | Entire WKWebView is made transparent while loading, even if it can retain content; every completed editor load refreshes all context. | Keep known usable document content visible during background reload where editor ownership allows it, and scope refresh to data actually invalidated by the editor. Do not claim offline editing without a durable document path. Test slow reload, save-before-close, and previously cached content. |
| P2 | Capture composition/list copy: `CaptureViews.swift:12,18,46` | Quick capture repeats its purpose with a decorative title and instructional empty-state prose. Populated search returns a blank list when no matches exist. | Keep the native title, focused editor, Save, and Close. Retain draft status only when it informs a real save state. Use a standard no-search-results state. Screen should devote its useful area to the user's text. |
| P2 | Legacy local daybook: `LocalDaybookView.swift:16-27,56-72` | A second date header, day picker, event preview and large Today heading recreate the connected day screen inside a legacy editor. iOS title is blank, making ownership unclear. | Label the existing route Device notes. Retain date selection, the actual editor, truthful local-save status, and Share. Remove the duplicated upcoming-event preview; keep legacy content readable/exportable. Do not silently migrate or delete it. |

## Elements that earn their place

- The week strip selects a day; all-day separation, calendar colors, and event detail preserve scheduling meaning.
- The Today control recentres on the current time; the note preview opens the daily note and keeps the accepted swipe-up affordance.
- The sidebar provides one stable route to secondary workspaces. Keep a visible menu button in addition to the gesture.
- Task completion, due date, priority, title, revision-conflict state, and pending status affect a user's next action. Preserve them without exposing implementation vocabulary.
- Save errors, account identity, and offline status are necessary when accurate and scoped to the current operation.
- Local-note ownership must remain explicit until actual migration exists.

## Verification matrix after the task-creation blocker

- **Daily note:** create a task from the note; reload; see it in Tasks; complete from each surface; verify exactly one graph task and consistent checked state. Repeat offline only if that path is supported.
- **Phone home:** Dawn/Dark; day selection; Today recenter; all-day and overlapping events; calendar colors; sidebar button/swipe; note tap/pull-up; large text.
- **iPad workspace:** phone-equivalent Voice, meeting capture, tasks and note access at regular/compact widths; selection continuity.
- **Tasks:** new task with keyboard focus; date/priority edit; search across the promised scope; complete/reopen; delayed response; no connection; restart with pending edits; revision conflict resolution; no-account onboarding.
- **Captures:** empty and populated creation; retained draft on close; no-result search; canonical add-to-note without duplicates; failure retains source text.
- **Settings:** identify active account; automatic successful sign-in; actionable failure; sign-out; appearance persists; detailed storage copy stays out of routine account flow.
- **Device notes:** explicit local ownership, date changes preserve edits, local save errors are truthful, share works, no silent migration.

This audit supplies implementation order and acceptance criteria; it does not claim those improvements are built, deployed, or runtime-verified.
