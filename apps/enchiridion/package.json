{
  "name": "enchiridion",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": ["website", "services/*", "packages/*"],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:setup": "bun scripts/setup.ts",
    "dev:google": "bun scripts/setup.ts --google && bun scripts/dev.ts --google",
    "dev:website": "bun run --cwd website dev",
    "dev:oauth": "bun run --cwd services/oauth dev",
    "build": "bun run --cwd website build && bun run --cwd services/oauth build && bun run --cwd services/google-calendar build && bun run --cwd services/documents build",
    "check": "bun x --bun tsc --noEmit && bun run --cwd packages/oauth-client check && bun run --cwd services/oauth check && bun run --cwd services/google-calendar check && bun run --cwd services/documents check && bun run --cwd website check",
    "test": "bun test",
    "test:e2e": "bun scripts/e2e.ts",
    "db:migrate": "bun run --cwd services/oauth db:migrate && bun run --cwd services/google-calendar db:migrate && bun run --cwd services/documents db:migrate"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@cloudflare/workers-types": "latest",
    "@enchiridion/oauth-client": "workspace:*",
    "capnweb": "^0.12.0",
    "jose": "^6.2.12",
    "playwright": "^1.58.2",
    "undici": "^7.24.0",
    "ws": "^8.21.0",
    "typescript": "^6.0.3"
  }
}
