/** Generated policy fixture consumed by the routing audit and negative tests. */
export const REQUIRED_MUTATION_DISCOVERY_ADAPTERS = [
  "workspace-rpc", "user-do", "worker-fetch", "do-alarm", "service-sink", "migration-root", "tool"
] as const
export const TEMPORARY_COMPATIBILITY_SUNSET = "2026-12-31" as const
