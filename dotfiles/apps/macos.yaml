actions:
  - action: package.install
    list:
      - alt-tab
      - bartender
      - elgato-control-center
      - iterm2
      - secretive

  # May need a provider for mas
  # - action: package.install
  #   provider: brew
  #   extra-args: --mas
  #   list:
  #     - 1236045954 # Canary Mail
