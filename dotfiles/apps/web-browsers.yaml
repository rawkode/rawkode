actions:
  - action: package.repository
    provider: aptitude
    name: ppa:mozillateam/firefox-next
    where: os.type == "Ubuntu"

  - name: firefox-developer-edition
    action: package.install
    variants:
      - where: os.type == "Ubuntu"
        name: firefox

  - action: package.install
    name: google-chrome
