depends:
  - rust

actions:
  - action: package.install
    name: git

  - action: file.copy
    from: config
    to: "{{ user.home_dir }}/.config/git/config"

  - action: file.copy
    from: ignores
    to: "~/.config/git/ignores2"
    template: false
