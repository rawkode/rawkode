{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "wrangler publish --dry-run src/lib.rs",
    "dev": "wrangler dev src/lib.rs --local"
  },
  "devDependencies": {
    "wrangler": "^2.0.0"
  }
}
