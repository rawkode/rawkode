# Google OAuth

Google OAuth is optional product integration and must be re-enrolled through
the product's audited identity flow. Credentials are never repository values.
Infrastructure configuration and deployment use reviewed Alchemy.run v2
definitions; direct Wrangler deployment and secret-management steps are not
approved.
