{
  "name": "enchiridion",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "workers/*",
    "packages/*",
    "supertags/*"
  ],
  "scripts": {
    "typecheck": "tsc --build tsconfig.json --verbose",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
