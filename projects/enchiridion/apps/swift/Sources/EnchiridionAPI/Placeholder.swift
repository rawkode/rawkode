// Placeholder.swift
// EnchiridionAPI
//
// Out of scope for the "EnchiridionSync skeleton" task (see
// /Users/rawkode/.claude/plans/cheeky-greeting-lampson.md). This target is
// the thin GraphQL client against the vault worker's gateway, which doesn't
// exist yet — see README.md in this directory. This file exists only so
// the `EnchiridionAPI` product isn't empty, which would otherwise block
// `swift build` for every other target in this package.

/// Marker type confirming this module compiles ahead of its real
/// implementation.
public enum EnchiridionAPIPlaceholder {}
