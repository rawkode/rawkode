# EnchiridionCore

Native domain identifiers and task semantics. Cross-platform identifiers must
remain byte-compatible with the TypeScript contracts in `packages/`; this is
a greenfield contract, not a legacy-app compatibility promise.
