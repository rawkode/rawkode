# EnchiridionSync

The native CRDT and synchronization boundary. `LoroEngine`, the protocol, and
outbox are present; durable cross-process transactions and live convergence
proof remain required before production use.
