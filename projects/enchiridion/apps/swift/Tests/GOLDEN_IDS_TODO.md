# Cross-platform identifier fixtures

The fixture suite protects the current Swift and TypeScript contracts. It does
not establish compatibility with `apps/enchiridion`. Any identifier evolution
requires an explicit versioned contract and refreshed fixtures on both sides.
